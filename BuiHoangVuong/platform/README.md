# Student Support Platform — merged system

Two prototypes behind one login: the **In-Class** early-warning dashboard (risk scoring, ranking,
Vietnamese explanations) and the **Course-Long** support system (weekly progress, interventions, analytics).

```bash
docker compose up          # fe :8080 · be :8000 · db · redis · worker
```

Sign in with `teacher` / `teacher123`. See section 9 of the handover notes for verification commands.
